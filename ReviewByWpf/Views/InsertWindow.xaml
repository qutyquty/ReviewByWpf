<Window x:Class="ReviewByWpf.Views.InsertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReviewByWpf.Views"
        mc:Ignorable="d"
        Title="리뷰 등록" Height="400" Width="600">
    <Grid>
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" 
                Orientation="Vertical" Margin="5">
                <Image Source="{Binding PosterUrl}" Width="200" Height="250" Margin="5" />
                <Button Content="포스터 선택" Click="PosterSelectButton_Click" Width="200" Margin="5" />
                <Button Content="리뷰 저장" Command="{Binding SaveCommand}" Width="200" Margin="5" />
            </StackPanel>

            <TextBox Grid.Column="1" Grid.Row="0"
                Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                FontSize="14" Padding="5" Margin="0,0,0,3" />

            <TextBox Grid.Column="1" Grid.Row="1" 
                Text="{Binding Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                TextWrapping="Wrap" AcceptsReturn="True"
                FontSize="14" Padding="5" 
                VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
