<Window x:Class="ReviewByWpf.Views.TmdbSearchUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReviewByWpf.Views"
        xmlns:vm="clr-namespace:ReviewByWpf.ViewModles"
        mc:Ignorable="d"
        Title="TMDB 영화 검색" Height="600" Width="800">

    <Window.DataContext>
        <vm:TmdbSearchUpViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <!-- 검색어 입력, 검색 버튼 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="5" />
            <Button Content="검색" Command="{Binding SearchCommand}" Width="100" Margin="5" />
        </StackPanel>
        <!-- TMDB에서 가져온 포스터 리스트 -->
        <ListBox x:Name="WrapListBox" Grid.Row="1" ItemsSource="{Binding Posters}" 
                 SelectedItem="{Binding SelectedMTPoster}"
                 BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- WrapPanel은 유동적으로 가로 방향으로 배치 후 자동 줄바꿈 -->
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <!-- 포스터 표시용 템플릿 -->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2">
                        <Image Source="{Binding PosterUrl}" Width="150" Height="220" Margin="5" />
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 포스터 선택 -->
        <Button Grid.Row="2" Content="포스터 선택" Command="{Binding ConfirmCommand}" Margin="5" />
    </Grid>
</Window>
